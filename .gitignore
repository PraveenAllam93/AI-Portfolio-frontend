node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
# `.env*` is deliberately broad: `.env` + `.env.*` match only dot-separated
# variants, so a hand-named copy like `.env_from-praveen` stays UNIGNORED and
# `git add -A` would commit it. Matches the backend repo's rule. Do not narrow.
.env*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

.claudeignore
.gemini/
sample-media/

# IDEs and Tools
.vscode/
.idea/
.claude/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

CLAUDE.md

ideas/